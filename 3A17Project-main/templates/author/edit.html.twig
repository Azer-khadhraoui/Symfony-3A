{% extends 'base.html.twig' %}

{% block title %}Modifier l'auteur{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier l'auteur: {{ author.username }}</h1>

        {{ form_start(form) }}
            <div class="mb-3">
                {{ form_label(form.username) }}
                {{ form_widget(form.username) }}
                {{ form_errors(form.username) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.email) }}
                {{ form_widget(form.email) }}
                {{ form_errors(form.email) }}
            </div>

            <div class="mb-3">
                {{ form_widget(form.submit, {'label': 'Mettre à jour'}) }}
                <a href="{{ path('list_authors') }}" class="btn btn-secondary ms-2">Annuler</a>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}